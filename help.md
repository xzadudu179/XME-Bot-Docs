

# XME-Bot 使用文档

<!-- > 目前还 (几乎) 没有任何内容 -->

## 注意事项

---

### 提醒

- 请 **一定不要** 以及 **尽量避免** 让 bot 以任何形式输出敏感/违规字符，很容易导致 bot 被永久冻结！

  如果有任何形式导致 bot 因为用户输入的违规行为被封禁，一旦发现会 **追究责任** 且 **记录用户 qq 账号。**

- 该 bot 还在 **早期开发阶段**，很多功能很可能会变化很大。

- 默认权限暂为 **在群聊内**。

---

### 关于指令

> 关于指令详情参见[指令术语](glossary?id=%e6%8c%87%e4%bb%a4)

- XME-Bot 的指令默认以 `/`、`!`、 `！`、 `.` 中任意一个的字符开头，在开头字符前 **@ 机器人**或输入类似 `xme,` 之类可被解析为**呼叫机器人**的语句时，会被判定为 `to_me` 也就是**呼叫机器人**。
- 在帮助的指令参数相关里，`<>` 括起来的参数代表[可选参数](glossary?id=可选参数)， `()` 括起来的参数代表[必要参数](glossary?id=必要参数)， `[]` 括起来的参数代表[类 Shell 参数](glossary?id=类-shell-参数)。

---

#### 指令格式

- 在指令列表中，指令格式为：

  **指令名**

  - **作用**

    关于指令是干什么用的。

  - **用法**

    关于指令的具体使用语法，例如：
    ``` Text
    /指令名 (必选参数) <可选参数> [类Shell参数]
    ```

  - **权限/可用范围**

    关于指令所需的[权限](glossary?id=权限)。若没有则为 `无`。若有会写出具体条件，需要符合条件才可调用指令。

  - **频率/时间限制**

    关于指令的频率与时间限制，例如一天只能调用一次之类的。

    该项说明为可选项，只有拥有限制的才会说明。

- **别名**

    关于指令的别名，使用指令的别名也可以调用指令。

    比如 `help` 指令有 `帮助` 别名，所以  使用 `/帮助` 和使用 `/help` 并没有区别。

---

#### 插件格式

- 有些指令被分类在插件里，插件的格式为：

  **插件名**

  - **插件介绍**

    关于插件的一些功能介绍

  - **指令列表：**

    指令会被写在这里

---

## 功能列表

---

### [插件] 今日老婆

- **插件介绍**

  查看自己的今日老婆，或者是别人的今日老婆。

- **指令列表：**

  #### wife

  - **作用**

    查看自己或别人的今日老婆群员。
    > 若参数填写为 `at` 则会同时 @ 你今日的老婆

  - **用法**

  ``` Text
  /wife <at想看的人>
  ```

  - **权限/可用范围**

    1. **在群聊内**

  - **别名**

    `今日老婆`、`kklp`、`看看老婆`。


---

### [插件] 实例控制

- **插件介绍**

  查看自己的今日老婆，或者是别人的今日老婆。

- **指令列表：**

  #### inst_restart

  - **作用**

    重新启动 bot 实例。

  - **用法**

  ``` Text
  /restart
  ```

  - **权限/可用范围**

    1. **需要 @ bot 或是呼叫 bot**
    2. **是 SUPERUSER**

  - **别名**

    `inst_重启`、 `inst_rebot`、 `inst_reboot`、 `instRestart`、 `instRebot`、 `instReboot`

  ---

  #### inst_kill

  - **作用**

    杀死 bot 实例。

  - **用法**

  ``` Text
  /kill
  ```

  - **权限/可用范围**

    1. **需要 @ bot 或是呼叫 bot**
    2. **是 SUPERUSER**

  - **别名**

    `inst_关机`、`inst_shutdown`

  ---



### [插件] 骰子

- **插件介绍**

  自定义面数或者规则的骰子。

- **指令列表：**

  #### dice

  - **作用**

    投出默认1个指定面数 （最大1亿面）的骰子。

  - **用法**

  ``` Text
  /dice (骰子面数) <骰子数量>
  ```

  - **权限/可用范围**

    默认权限。

  - **别名**

    `d`。

---

### [插件] 漂流瓶

- **插件介绍**

  扔/捡来自各个群组的漂流瓶。

  > 神秘力量入侵了大海...现在每捡一次漂流瓶都会让它更加混乱，也更容易碎掉...但是点赞可以让混乱值变少...

- **指令列表：**

  #### throw

  - **作用**

    扔漂流瓶。如果漂流瓶内容太过接近会不允许扔出。

    > 注意：请不要往漂流瓶里写违规信息！

  - **用法**

  ``` Text
  /throw (漂流瓶内容)
  ```

  - **权限/可用范围**

    1. **在群聊内**

  - **别名**

    `扔瓶子`、 `扔漂流瓶`、 `扔瓶`。

  ---

  #### pickup

  - **作用**

    捡漂流瓶（90%概率捡到）

  - **用法**

  ``` Text
  /pickup
  ```

  - **权限/可用范围**

    1. **在群聊内**

  - **别名**

    `捡瓶子`、 `捡漂流瓶`、 `捡瓶`、 `pick`。

---



### [指令] answer

- **作用**

  随机翻开答案之书的一页，并且返回内容。
  "心中默念你的问题，将会得到你的答案。"

- **用法**

``` Text
/answer
```

- **权限/可用范围**

  默认权限。

- **别名**

  `答案之书`、 `ans`

---

### [指令] bot_leave

- **作用**

  使机器人离开当前群组。

- **用法**

``` Text
/bot_leave
```

- **权限/可用范围**

  1. **需要 @ bot 或是呼叫 bot**
  2. **是群主或是 SUPERUSER**
  3. **在群聊内**

- **别名**

  `bot_退群`、 `bot_退出群聊`、 `bot_exit`

---

### [指令] choice

- **作用**

  指定一个事情列表，bot 会随机挑选一件并返回。

- **用法**

``` Text
/choice (事情列表 (空格分隔))
```

- **权限/可用范围**

  默认权限。

- **别名**

  `选择`、 `cho`、 `决定`

---

### [指令] game

- **作用**

  游玩一个小游戏，游戏参数格式为：`参数名=参数值（以逗号分隔）`

  例如：`r=1~10,t=5`
  <!-- 小游戏的列表使用 `/game -h` 以查看 -->
  目前拥有以下游戏：
  - `guess`   猜数字游戏

- **用法**

``` Text
/game (小游戏名) [OPTIONS]
OPTIONS:
	-h, --help	查看帮助
	-a, --args	指定小游戏的参数
	-i, --info	查看你输入的小游戏的帮助而不是游玩
```

- **权限/可用范围**

  默认权限。

- **别名**

  `游戏`、 `小游戏`、 `play`、 `玩`。

---

### [指令] help

- **作用**

  显示指令的帮助信息。

- **用法**

``` Text
/help <指令名>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `usage`、 `docs`、 `帮助`。

---

### [指令] httpcode

- **作用**

  查询 http 状态码并且返回一张来自 [http.cat](https://http.cat) 的猫猫图。

- **用法**

``` Text
/httpcode <状态码>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `http`、 `http状态码`。

---


### [指令] jrrp

- **作用**

  查看当前 qq 号今日的人品或群友人品排名。

  参数填写整数，正数为人品最高排名，负数为最低。填写 `avg` 为群员平均值

- **用法**

``` Text
/jrrp <参数>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `今日人品` 、 `luck`

---

### [指令] lottery

- **作用**

  花费指定量的金币，抽取最多 1.79 倍量的金币。

  金币最多设置为 50。

- **用法**

``` Text
/lottery (金币数量)
```

- **权限/可用范围**

  默认权限。

- **频率/时间限制**

  每日 5 次。

- **别名**

  `抽奖`、 `lot`。

---

### [指令] prevrecall

- **作用**

  查询 / 启用 / 关闭 群聊的防撤回功能。

- **用法**

``` Text
/prevrecall <开|关|T|F>
```

- **权限/可用范围**

  1. **在群聊内**
  2. **调用者是 SUPERUSER**

- **别名**

  `防撤回`、 `precall`、 `防撤`、 `防撤回功能`。

---

### [指令] sign

- **作用**

  签到，并且得到一定量的金币。

- **用法**

``` Text
/sign
```

- **权限/可用范围**

  默认权限。

- **频率/时间限制**

  每日 1 次。

- **别名**

  `签到`、 `check`、 `checkin`、 `register`、 `s`。

---

### [指令] status

- **作用**

  查看运行该 XME-Bot 实例的设备的系统状态。

- **用法**

``` Text
/status
```

- **权限/可用范围**

  默认权限。

- **别名**

  `系统状态`、 `stats`。

---
<!--
### [指令] translate

- **作用**

  使用 ChatGLM-4 模型进行文本翻译，参数可填如英语 法语 俄语 中文等。
  调用时的语句是 `用户：将以下内容翻译成[你写的参数]：`
  > 注意：该指令可能将会在不久后删除

- **用法**

``` Text
/translate (需要翻译的文本内容) [OPTIONS]
OPTIONS:
	-h, --help	查看帮助
	-l, --language	指定要翻译成的语言
```

- **权限/可用范围**

  默认权限。

- **别名**

  `翻译`、 `trans`。

--- -->

### [指令] title

- **作用**

  为一个群员设置指定的头衔（需要 bot 是群主权限）。

- **用法**

``` Text
/title (at想要设置的群友) (头衔名 填写 -delete 删除头衔)
```

- **权限/可用范围**

  默认权限。

- **别名**

  `set_title`、 `设置头衔`、 `头衔`。

---

### [指令] userinfo

- **作用**

  查看自己当前被记录的个人信息，比如金币数量等。如果 at 人则会查看那个人的个人信息。

- **用法**

``` Text
/userinfo <at人>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `个人信息`、 `个人资料`、 `uinfo`、 `info`。

---

### [指令] weather

- **作用**

  查看指定地区的天气。

  天气 API 来源于[高德开放平台](https://lbs.amap.com/api/webservice/guide/api/weatherinfo)。

- **用法**

``` Text
/weather <地区名> <未来天气预测天数(1~3)>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `天气`、 `wea`、 `查看天气`。

---

### [指令] whoru

- **作用**

  查看 bot 当前使用的角色信息。

- **用法**

``` Text
/whoru
```

- **权限/可用范围**

  默认权限。

- **别名**

  `你是谁`、 `who`、 `chac`。

---

### [指令] xmeposts

- **作用**

  通过 RSS 订阅并查看九九最近的 n 个文章，默认 1 个。

- **用法**

``` Text
/xmeposts <文章数>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `九九文章`、 `rss179`、 `posts179`、 `blogposts`、 `posts`、 `post`。

---

<!--
## [特殊] XME

### 插件介绍

  XME 虚空之领宇宙相关功能 \n（注意：所有相关功能都需要 at 机器人或者输入 xme 前缀 例如 xme.r）

  > 注意：该插件还在内部测试中，部分指令可能并不在文档里。

### 指令列表

#### register

- **作用**

  签到 / 注册账号并签到。

- **用法**

``` Text
/register
```

- **权限/可用范围**

  1. **需要 @ bot 或是呼叫 bot**

- **别名**

  `reg`、 `r`。

---


 -->





## 其他

### 帮助编写文档

如果你对文档的内容有想要调整的地方或是建议，欢迎前往文档的 [Github 仓库](https://github.com/xzadudu179/XME-Bot-Docs) 进行合作哦！

对文档做出贡献的人会被放在最下面的特别感谢里~

### 关于九镹

<div style="max-width:400px;margin:left">

![九镹](https://image.179.life/images/179.png)

</div>
↑ 九镹.png ↑

九镹是 XME-Bot 的开发者，是一个学计算机不太久的新人 owo。

要是好奇的话，可以来 [这里](https://blog.xzadudu179.top/about/) 看看九九的个人介绍哦！

## 特别感谢

- 每一个喜欢使用 XME-Bot 的用户