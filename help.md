

# XME-Bot 使用文档

<!-- > 目前还 (几乎) 没有任何内容 -->

## 注意事项

---

### 提醒

- 请 **一定不要** 以及 **尽量避免** 让 bot 以任何形式输出敏感/违规字符，很容易导致 bot 被永久冻结！

  如果有任何形式导致 bot 因为用户输入的违规行为被封禁，一旦发现会 **追究责任** 且 **记录用户 qq 账号。**

- 该 bot 还在 **早期开发阶段**，很多功能很可能会变化很大。

- 默认权限暂为 **在群聊内**。

---

### 关于指令

> 关于指令详情参见[指令术语](glossary?id=%e6%8c%87%e4%bb%a4)

- XME-Bot 的指令默认以 `/`、`!`、 `！`、 `.` 中任意一个的字符开头，在开头字符前 **@ 机器人**或输入类似 `xme,` 之类可被解析为**呼叫机器人**的语句时，会被判定为 `to_me` 也就是**呼叫机器人**。
- 在帮助的指令参数相关里，`<>` 括起来的参数代表[可选参数](glossary?id=可选参数)， `()` 括起来的参数代表[必要参数](glossary?id=必要参数)， `[]` 括起来的参数代表[类 Shell 参数](glossary?id=类-shell-参数)。

---

#### 指令格式

- 在指令列表中，指令格式为：

  **指令名**

  - **作用**

    关于指令是干什么用的。

  - **用法**

    关于指令的具体使用语法，例如：
    ``` Text
    /指令名 (必选参数) <可选参数> [类Shell参数]
    ```

  - **权限/可用范围**

    关于指令所需的[权限](glossary?id=权限)。若没有则为 `无`。若有会写出具体条件，需要符合条件才可调用指令。

- **别名**

    关于指令的别名，使用指令的别名也可以调用指令。

    比如 `help` 指令有 `帮助` 别名，所以  使用 `/帮助` 和使用 `/help` 并没有区别。

---

#### 插件格式

- 有些指令被分类在插件里，插件的格式为：

  **插件名**

  - **插件介绍**

    关于插件的一些功能介绍

  - **指令列表：**

    指令会被写在这里

---

## 指令列表

---

### [插件] 今日老婆

- **插件介绍**

  查看自己的今日老婆，或者是别人的今日老婆。

- **指令列表：**

  #### cancanneedwife

  - **作用**

    查看指定群员今日的老婆群员。

  - **用法**

  ``` Text
  /cancanneedwife (@一个群友)
  ```

  - **权限/可用范围**

    1. **在群聊内**

  - **别名**

    `看看老婆`、 `peekwife`、 `kkndwife`、 `kkndlp`、 `kklp`。

  ---

  #### wife

  - **作用**

    查看自己或别人的今日老婆群员。

  - **用法**

  ``` Text
  /wife <at想看的人>
  ```

  - **权限/可用范围**

    1. **在群聊内**

  - **别名**

    `今日老婆`、`kklp`、`看看老婆`。


---

### [插件] 实例控制

- **插件介绍**

  查看自己的今日老婆，或者是别人的今日老婆。

- **指令列表：**

  #### inst_restart

  - **作用**

    重新启动 bot 实例。

  - **用法**

  ``` Text
  /restart
  ```

  - **权限/可用范围**

    1. **需要 @ bot 或是呼叫 bot**
    2. **是 SUPERUSER**

  - **别名**

    `inst_重启`、 `inst_rebot`、 `inst_reboot`、 `instRestart`、 `instRebot`、 `instReboot`

  ---

  #### inst_kill

  - **作用**

    杀死 bot 实例。

  - **用法**

  ``` Text
  /kill
  ```

  - **权限/可用范围**

    1. **需要 @ bot 或是呼叫 bot**
    2. **是 SUPERUSER**

  - **别名**

    `inst_关机`、`inst_shutdown`

  ---



### [插件] 骰子

- **插件介绍**

  自定义面数或者规则的骰子。

- **指令列表：**

  #### dice

  - **作用**

    投出默认1个指定面数 （最大1亿面）的骰子。

  - **用法**

  ``` Text
  /dice (骰子面数) <骰子数量>
  ```

  - **权限/可用范围**

    默认权限。

  - **别名**

    `d`。

---

### [插件] 漂流瓶

- **插件介绍**

  扔/捡来自各个群组的漂流瓶。

- **指令列表：**

  #### throw

  - **作用**

    扔漂流瓶。

    > 注意：请不要往漂流瓶里写违规信息！

  - **用法**

  ``` Text
  /throw (漂流瓶内容)
  ```

  - **权限/可用范围**

    1. **在群聊内**

  - **别名**

    `扔瓶子`、 `扔漂流瓶`、 `扔瓶`。

  ---

  #### pickup

  - **作用**

    捡漂流瓶（50%概率捡到）

  - **用法**

  ``` Text
  /pickup
  ```

  - **权限/可用范围**

    1. **在群聊内**

  - **别名**

    `捡瓶子`、 `捡漂流瓶`、 `捡瓶`、 `pick`。

---



### answer

- **作用**

  随机翻开答案之书的一页，并且返回内容。
  "心中默念你的问题，将会得到你的答案。"

- **用法**

``` Text
/answer
```

- **权限/可用范围**

  默认权限。

- **别名**

  `答案之书`、 `ans`

---

### bot_leave

- **作用**

  使机器人离开当前群组。

- **用法**

``` Text
/bot_leave
```

- **权限/可用范围**

  1. **需要 @ bot 或是呼叫 bot**
  2. **是群主或是 SUPERUSER**
  3. **在群聊内**

- **别名**

  `bot_退群`、 `bot_退出群聊`、 `bot_exit`

---

### help

- **作用**

  显示指令的帮助信息。

- **用法**

``` Text
/help <指令名>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `usage`、 `docs`、 `帮助`。

---

### httpcode

- **作用**

  查询 http 状态码并且返回一张来自 [http.cat](https://http.cat) 的猫猫图。

- **用法**

``` Text
/httpcode <状态码>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `http`、 `http状态码`。

---


### jrrp

- **作用**

  查看当前 qq 号今日的人品~ [by 千枫]

- **用法**

``` Text
/jrrp
```

- **权限/可用范围**

  默认权限。

- **别名**

  `今日人品` 、 `luck`

---

### prevrecall

- **作用**

  查询 / 启用 / 关闭 群聊的防撤回功能。

- **用法**

``` Text
/prevrecall <开|关|T|F>
```

- **权限/可用范围**

  1. **在群聊内**
  2. **调用者是 SUPERUSER**

- **别名**

  `防撤回`、 `precall`、 `防撤`、 `防撤回功能`。

---


### status

- **作用**

  查看运行该 XME-Bot 实例的设备的系统状态。

- **用法**

``` Text
/status
```

- **权限/可用范围**

  默认权限。

- **别名**

  `系统状态`、 `stats`。

---

### translate

- **作用**

  使用 ChatGLM-4 模型进行文本翻译，参数可填如英语 法语 俄语 中文等。
  调用时的语句是 `用户：将以下内容翻译成[你写的参数]：`

- **用法**

``` Text
/translate <语言名>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `翻译`、 `trans`。

---

### weather

- **作用**

  查看指定地区的天气。

  天气 API 来源于[高德开放平台](https://lbs.amap.com/api/webservice/guide/api/weatherinfo)。

- **用法**

``` Text
/weather <地区名> <未来天气预测天数(1~3)>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `天气`、 `wea`、 `查看天气`。

---

### xmerss

- **作用**

  通过 RSS 订阅并查看九九最近的 n 个文章，默认 5 个。

- **用法**

``` Text
/xmerss <文章数>
```

- **权限/可用范围**

  默认权限。

- **别名**

  `九九文章`、 `rss179`、 `atom179`、 `xmeatom`、`rss`。

---

## [特殊] XME

### **插件介绍**

  XME 虚空之领宇宙相关功能 \n（注意：所有相关功能都需要 at 机器人或者输入 xme 前缀 例如 xme.r）

  > 注意：该插件还在内部测试中，部分指令可能并不在文档里。

### **指令列表**

#### register

- **作用**

  签到 / 注册账号并签到。

- **用法**

``` Text
/register
```

- **权限/可用范围**

  1. **需要 @ bot 或是呼叫 bot**

- **别名**

  `reg`、 `r`。

---








## 其他

### 帮助编写文档

如果你对文档的内容有想要调整的地方或是建议，欢迎前往文档的 [Github 仓库](https://github.com/xzadudu179/XME-Bot-Docs) 进行合作哦！

对文档做出贡献的人会被放在最下面的特别感谢里~

### 关于九镹

<div style="max-width:400px;margin:left">

![九镹](https://image.179.life/images/179.png)

</div>
↑ 九镹.png ↑

九镹是 XME-Bot 的开发者，是一个学计算机不太久的新人 owo。

要是好奇的话，可以来 [这里](https://blog.xzadudu179.top/about/) 看看九九的个人介绍哦！

## 特别感谢

- 每一个喜欢使用 XME-Bot 的用户